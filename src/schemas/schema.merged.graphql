schema {
  query: Query
}

type Query {
  hello(name: String): String!
  people(pagination: paginationInput): [Person]!
}

input paginationInput {
  page: Int!
  perPage: Int!
}

type Person {
  name: String!
  born: Int
}
