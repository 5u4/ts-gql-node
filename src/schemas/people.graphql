type Person {
    identity: ID!
    name: String!
    born: Int
    movies(direction: PersonMovieRelation, pagination: paginationInput): [Movie]!
}

type Query {
    people(pagination: paginationInput): [Person]!
}
